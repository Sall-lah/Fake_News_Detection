---
phase: quick
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: [app.py]
autonomous: true
requirements: []
---

<objective>
Suppress the sklearn Pipeline LGBMClassifier feature-name UserWarning during prediction by wrapping the `model.predict()` call with a targeted warnings filter.

Purpose: Eliminate noisy console warnings that are harmless (input is raw text for the TF-IDF vectorizer, not a pre-vectorized array) but clutter logs.
Output: Modified `app.py` with warning suppression scoped only to the prediction call.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@app.py
</context>

<tasks>

<task type="auto">
  <name>Add targeted warning suppression around model.predict()</name>
  <files>app.py</files>
  <action>
    1. Add `import warnings` to the top-level imports (after line 8, alongside existing stdlib imports).
    2. Replace the bare `model.predict([cleaned])[0]` call on line 51 with a `warnings.catch_warnings()` context manager that:
       - Calls `warnings.simplefilter("ignore", category=UserWarning)` inside the context
       - Executes `prediction = model.predict([cleaned])[0]` inside the context
       - Keeps the existing `try/except Exception` wrapper around the entire context block (or place the context block inside the try)
    3. The suppression must be scoped ONLY to this predict call — do NOT use `warnings.filterwarnings` at module level.
  </action>
  <verify>
    <automated>python -c "from app import app; import warnings; warnings.filterwarnings('error'); r = app.test_client().post('/predict', json={'title':'test','text':'hello world'}); print(r.status_code, r.get_json())"</automated>
  </verify>
  <done>
    - `import warnings` present at top of app.py
    - `model.predict([cleaned])` wrapped in `warnings.catch_warnings()` with `simplefilter("ignore", UserWarning)`
    - Existing try/except error handling preserved
    - POST /predict returns 200 with correct label, no UserWarning emitted
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST /predict input | User-supplied JSON (title, text) flows into clean_text → model.predict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Denial of Service | /predict endpoint | accept | Local-only API per project constraints; no rate limiter required |
| T-quick-02 | Information Disclosure | Warning suppression | accept | Suppressing UserWarning does not hide security-relevant errors; the try/except still catches Exception-level failures |
</threat_model>

<verification>
- `python -c` test with warnings set to error mode confirms no UserWarning is raised
- POST /predict with valid input returns `{"status": "ok", "label": "..."}` with 200
- POST /predict with empty input returns 400
- POST /predict with no model loaded returns 503
</verification>

<success_criteria>
- No UserWarning about feature names appears during prediction
- All existing endpoint behavior unchanged
- Warning suppression is narrowly scoped (not module-wide)
</success_criteria>

<output>
After completion, create `.planning/quick/260517-193-fix-lgbmclassifier-feature-name-warning-/260517-193-SUMMARY.md`
</output>
