---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app.py, model.py, train.py, model.pkl]
autonomous: true
requirements: []
---

<objective>
Test server cold start behavior when model.pkl is absent from project root — verify training triggers, completes, and the prediction endpoint works afterward.

Purpose: Confirm the fallback training path works end-to-end so the app is resilient to a missing model file.
Output: Verified model.pkl created in root, prediction endpoint returns valid classification.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@app.py
@model.py
@train.py

<interfaces>
Key behavior contract (from codebase analysis):

- `model.py` `load_model()` checks `Path(__file__).parent / "model.pkl"` (project root only)
- If `model.pkl` absent → calls `train.py` to train fresh model
- `train.py` runs RandomizedSearchCV (5 candidates, 2-fold CV) → ~5+ minutes
- After training, `model.pkl` is saved to project root
- `app.py` exposes prediction endpoint (POST with title+text payload)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cold-start test — remove model.pkl, start server, wait for training, test prediction</name>
  <files>app.py, model.py, train.py, model.pkl</files>
  <action>
1. Remove model.pkl from project root if it exists: `Remove-Item -LiteralPath "model.pkl" -ErrorAction SilentlyContinue`
2. Verify it's gone: `Test-Path "model.pkl"` should return False
3. Start the Flask server using the conda env: `& "C:\Users\LENOVO\miniconda3\envs\fake_news_detection\python.exe" app.py` — run as a background process
4. Monitor server output for training completion signals (look for training logs, "model saved", or server ready message). Training takes ~5+ minutes due to RandomizedSearchCV with 5 candidates and 2-fold CV.
5. Once the server is ready (listening on its port, default 5000), test the prediction endpoint with a sample payload: `curl -X POST http://localhost:5000/predict -H "Content-Type: application/json" -d '{"title":"Test headline","text":"Test article body"}'`
6. Verify model.pkl was recreated: `Test-Path "model.pkl"` should return True
7. Stop the server process after verification completes.
  </action>
  <verify>
    <automated>Test-Path "model.pkl" -and (curl -s -X POST http://localhost:5000/predict -H "Content-Type: application/json" -d '{"title":"test","text":"test"}' | Select-String -Pattern '"prediction"')</automated>
  </verify>
  <done>model.pkl exists in project root after cold start, prediction endpoint returns a valid response with a "prediction" field (fake or true classification), server started without errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localhost → Flask API | Local-only; no external exposure. Input via POST body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | model.pkl (generated file) | accept | File is generated locally by train.py; no integrity concern for local-only test. |
| T-quick-02 | Information Disclosure | Training logs / stderr | accept | Local conda env, no PII in training output. |
</threat_model>

<verification>
- model.pkl absent before server start
- Server starts and triggers training automatically
- Training completes within reasonable time (~5-10 min)
- Prediction endpoint responds with valid classification
- model.pkl exists in project root after test
</verification>

<success_criteria>
- Cold start path executes without unhandled exceptions
- model.pkl is created in project root by the training fallback
- Prediction endpoint returns a structured response with classification result
- Server is functional after training completes
</success_criteria>

<output>
After completion, create `.planning/quick/260517-1dq-test-server-cold-start-without-model-pkl/260517-1dq-SUMMARY.md`
</output>
