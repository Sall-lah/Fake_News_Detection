---
phase: 03-dependencies-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
autonomous: true
requirements:
  - DEP-01
  - DEP-02

must_haves:
  truths:
    - "requirements.txt contains pandas==3.0.3"
    - "requirements.txt contains lightgbm==4.6.0 with provenance comment"
    - "requirements.txt has '# Requires Python 3.13' comment at the top"
    - "All ML dependencies (pandas, lightgbm, scikit-learn, numpy, scipy, joblib) are pinned with =="
    - "pip install -r requirements.txt completes without errors"
    - "import pandas and import lightgbm succeed in Python"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned dependency list with comments"
      contains:
        - "pandas==3.0.3"
        - "lightgbm==4.6.0"
        - "# Requires Python 3.13"
        - "# Pinned to match model.pkl training provenance"
  key_links:
    - from: "requirements.txt"
      to: "pip install -r requirements.txt"
      via: "package installation"
      pattern: "pip install -r requirements.txt"
    - from: "requirements.txt"
      to: "Dockerfile"
      via: "COPY + pip install reference"
      pattern: "requirements.txt"
---

<objective>
Update requirements.txt with pandas==3.0.3 and pin lightgbm==4.6.0, then install all dependencies and verify imports succeed.

Purpose: Prevent pickle incompatibility by pinning all ML-critical dependencies to known-compatible versions.
Output: Updated requirements.txt with comments, verified installation via import smoke test.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dependencies-foundation/03-CONTEXT.md
@requirements.txt
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update requirements.txt with pinned dependencies and comments</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (current state)
    - .planning/phases/03-dependencies-foundation/03-CONTEXT.md (locked decisions D-01 through D-06)
  </read_first>
  <action>
Edit requirements.txt to produce the following exact content:

```
# Requires Python 3.13
Flask==3.1.1
scikit-learn==1.7.1
numpy==2.4.4
scipy==1.17.1
joblib==1.5.3
pandas==3.0.3
lightgbm==4.6.0  # Pinned to match model.pkl training provenance
```

Specific changes:
1. Add `# Requires Python 3.13` as the first line (per D-02)
2. Add `pandas==3.0.3` after joblib (per DEP-01 / D-01)
3. Change `lightgbm` (unpinned) to `lightgbm==4.6.0  # Pinned to match model.pkl training provenance` (per DEP-02 / D-05)
4. Keep all existing deps (Flask==3.1.1, scikit-learn==1.7.1, numpy==2.4.4, scipy==1.17.1, joblib==1.5.3) unchanged (per D-01 — Flask transitive deps remain unpinned)
5. Keep all dependencies in a single file — no requirements-dev.txt (per D-03)
  </action>
  <verify>
    <automated>python -c "
content = open('requirements.txt').read()
assert '# Requires Python 3.13' in content, 'Missing Python version comment'
assert 'pandas==3.0.3' in content, 'Missing pandas pin'
assert 'lightgbm==4.6.0' in content, 'Missing lightgbm pin'
assert '# Pinned to match model.pkl training provenance' in content, 'Missing provenance comment'
assert content.count('lightgbm') == 1, 'Duplicate lightgbm entry'
print('requirements.txt validation passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt starts with "# Requires Python 3.13" comment
    - requirements.txt contains "pandas==3.0.3"
    - requirements.txt contains "lightgbm==4.6.0" with "# Pinned to match model.pkl training provenance" inline comment
    - requirements.txt contains all 7 dependencies: Flask, scikit-learn, numpy, scipy, joblib, pandas, lightgbm
    - All ML-critical deps use == pinning format
    - No requirements-dev.txt or other split files created
  </acceptance_criteria>
  <done>requirements.txt updated with pandas==3.0.3, lightgbm pinned to 4.6.0 with provenance comment, Python version comment at top, all existing deps preserved</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install dependencies and verify imports</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (updated by Task 1)
  </read_first>
  <action>
Run `pip install -r requirements.txt` to install all dependencies. Then run an import smoke test to verify pandas and lightgbm can be imported successfully (per D-04).

Use: `pip install -r requirements.txt`
Then: `python -c "import pandas; import lightgbm; print('Imports successful')"`
  </action>
  <verify>
    <automated>pip install -r requirements.txt && python -c "import pandas; import lightgbm; print('pandas', pandas.__version__); print('lightgbm', lightgbm.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - pip install -r requirements.txt exits with code 0
    - python -c "import pandas" exits with code 0
    - python -c "import lightgbm" exits with code 0
    - Output shows pandas version 3.0.3 and lightgbm version 4.6.0
  </acceptance_criteria>
  <done>pip install completes without errors, import pandas and import lightgbm both succeed with correct versions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local environment | Package downloads from PyPI are trusted via pinned versions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | requirements.txt | mitigate | All ML-critical packages pinned to exact versions (==) to prevent supply chain version drift; provenance comment on lightgbm documents training-time match. |
| T-03-02 | Repudiation | requirements.txt | accept | Local-only API with no audit requirements; git history provides change tracking. |
| T-03-03 | Information Disclosure | requirements.txt | accept | Dependency list is not sensitive data; no secrets or credentials in requirements.txt. |
</threat_model>

<verification>
1. requirements.txt contains all 7 dependencies with correct pins
2. `pip install -r requirements.txt` completes without errors
3. `import pandas` succeeds with version 3.0.3
4. `import lightgbm` succeeds with version 4.6.0
5. No duplicate entries in requirements.txt
6. Python version comment present at top of file
</verification>

<success_criteria>
- requirements.txt updated with pandas==3.0.3 and lightgbm==4.6.0 (pinned)
- All ML dependencies pinned with == format
- pip install succeeds without errors
- Import smoke test passes for pandas and lightgbm
- File contains required comments (Python version, lightgbm provenance)
</success_criteria>

<output>
After completion, create `.planning/phases/03-dependencies-foundation/03-01-SUMMARY.md`
</output>
