---
phase: 01-core-local-inference-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - preprocess.py
  - model.py
autonomous: true
requirements:
  - PRE-01
  - PRE-02
  - PRE-03
  - PRE-04
  - PRE-05
  - PRE-06
  - RUN-01
must_haves:
  truths:
    - "Preprocessing combines title+text and produces normalized cleaned text per rules"
    - "Model is loaded once at startup and cached for reuse"
  artifacts:
    - path: "preprocess.py"
      provides: "clean_text preprocessing pipeline"
      exports: ["clean_text"]
    - path: "model.py"
      provides: "model loader and cached instance"
      exports: ["load_model_or_exit", "get_model"]
  key_links:
    - from: "preprocess.py"
      to: "clean_text"
      via: "exported function"
      pattern: "def clean_text\\("
    - from: "model.py"
      to: "MODEL"
      via: "module-level cache"
      pattern: "MODEL\\s*="
---

<objective>
Build the preprocessing pipeline and model-loading module that the API will use.

Purpose: Ensure deterministic text cleaning and a single startup model load per D-03.
Output: preprocess.py and model.py modules.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-local-inference-api/01-CONTEXT.md
@.planning/phases/01-core-local-inference-api/01-RESEARCH.md
@.planning/phases/01-core-local-inference-api/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic preprocessing pipeline</name>
  <read_first>
    preprocess.py (if exists)
    .planning/phases/01-core-local-inference-api/01-CONTEXT.md
    .planning/REQUIREMENTS.md
  </read_first>
  <files>preprocess.py</files>
  <behavior>
    - clean_text("Hello", "World!") returns "hello world" (lowercase, non-alpha removed, normalized)
    - clean_text("A", "the a") removes stopwords and deduplicates preserving first occurrence
    - clean_text(None, None) returns "" after normalization
  </behavior>
  <action>
    Create preprocess.py with a single public function `clean_text(title: str | None, text: str | None) -> str` that implements D-07..D-11 and PRE-01..PRE-06.
    Steps (in order):
    1) Treat missing fields as empty strings (per D-07).
    2) Combine as `f"{title} {text}"` (D-07).
    3) Lowercase the combined string (D-08 / PRE-02).
    4) Replace non-alphabetic characters with spaces using `re.sub(r"[^a-z]", " ", value)` (D-08 / PRE-03).
    5) Normalize whitespace with `re.sub(r"\s+", " ", value).strip()` (D-11 / PRE-06).
    6) Remove stopwords using `from sklearn.feature_extraction.text import ENGLISH_STOP_WORDS` (D-09 / PRE-04).
    7) Deduplicate words while preserving first occurrence order (D-10 / PRE-05) using a `seen` set and ordered list.
    8) Normalize whitespace again after stopword removal + dedupe (D-11 / PRE-06).
    Ensure the module exports only `clean_text` in `__all__`.
  </action>
  <verify>
    <automated>python -m compileall .</automated>
  </verify>
  <acceptance_criteria>
    - preprocess.py contains `def clean_text(` with type hints for `title` and `text`
    - preprocess.py imports `ENGLISH_STOP_WORDS` from `sklearn.feature_extraction.text`
    - preprocess.py uses `re.sub(r"[^a-z]"` and `re.sub(r"\s+"` for cleanup
    - preprocess.py implements deduplication with a `seen` set and ordered output list
  </acceptance_criteria>
  <done>clean_text implements D-07..D-11 and PRE-01..PRE-06 deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create startup model loader with cached instance</name>
  <read_first>
    model.py (if exists)
    .planning/phases/01-core-local-inference-api/01-CONTEXT.md
    .planning/REQUIREMENTS.md
  </read_first>
  <files>model.py</files>
  <behavior>
    - load_model_or_exit sets a module-level cache and exits on failure
    - get_model returns the cached model or None
  </behavior>
  <action>
    Create model.py with:
    - A module-level `MODEL = None` cache.
    - `def load_model_or_exit(model_path: Path) -> object:` that calls `joblib.load(model_path)`, assigns to `MODEL`, and returns the model.
    - On any exception, log a generic message (no filename per D-06), then `raise SystemExit(1)` (D-04 fail-fast).
    - `def get_model() -> object | None:` returns `MODEL` without loading (D-03 single load reuse).
    Use `from pathlib import Path` and `import joblib`.
  </action>
  <verify>
    <automated>python -m compileall .</automated>
  </verify>
  <acceptance_criteria>
    - model.py defines `MODEL = None` at module scope
    - model.py defines `load_model_or_exit(` and calls `joblib.load(`
    - model.py raises `SystemExit(1)` on load failure with a message that does not include `model.pkl`
    - model.py defines `get_model(` that returns `MODEL`
  </acceptance_criteria>
  <done>Model loads once at startup and fails fast on error per D-03/D-04/D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preprocess input | Untrusted user text enters preprocessing pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | preprocess.py | mitigate | Sanitize non-alpha chars and normalize whitespace per D-08/D-11 before token processing. |
| T-01-02 | I (Information Disclosure) | model.py | mitigate | Generic load failure message without model filename (D-06). |
| T-01-03 | D (DoS) | model.py | accept | If model load fails, exit fast; local-only API, no retry loop. |
</threat_model>

<verification>
- `python -m compileall .` passes.
</verification>

<success_criteria>
- preprocess.py provides clean_text implementing all preprocessing rules.
- model.py provides cached model load and fail-fast behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-local-inference-api/01-01-SUMMARY.md`.
</output>
