---
phase: 01-core-local-inference-api
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app.py
  - requirements.txt
autonomous: true
requirements:
  - RUN-01
  - RUN-02
must_haves:
  truths:
    - "App loads model.pkl at startup and fails fast if loading fails"
    - "Runtime dependencies are pinned in requirements.txt"
  artifacts:
    - path: "app.py"
      provides: "startup load hook"
    - path: "requirements.txt"
      provides: "runtime dependency pins"
  key_links:
    - from: "app.py"
      to: "model.load_model_or_exit"
      via: "startup load"
      pattern: "load_model_or_exit"
    - from: "requirements.txt"
      to: "Flask, waitress, scikit-learn"
      via: "pinned versions"
      pattern: "flask==3.1.1|waitress==3.0.2|scikit-learn==1.7.1"
---

<objective>
Wire startup model loading and declare runtime dependencies.

Purpose: Ensure model is preloaded and runtime deps are pinned for local use.
Output: app startup hook and requirements.txt.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-local-inference-api/01-CONTEXT.md
@.planning/phases/01-core-local-inference-api/01-RESEARCH.md
@.planning/phases/01-core-local-inference-api/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add startup model load to app</name>
  <read_first>
    app.py
    model.py
    .planning/phases/01-core-local-inference-api/01-CONTEXT.md
  </read_first>
  <files>app.py</files>
  <action>
    Update app.py to load the model at startup using `load_model_or_exit` and the repo-local `model.pkl` path.
    Add imports:
    - `from pathlib import Path`
    - `from model import load_model_or_exit`
    Add module-level startup code after `app = Flask(__name__)`:
    ```python
    MODEL_PATH = Path(__file__).parent / "model.pkl"
    load_model_or_exit(MODEL_PATH)
    ```
    This must run on import to satisfy RUN-01 and D-03/D-04. Do not include the filename in any error messages (D-06 handled in model.py).
  </action>
  <verify>
    <automated>python -m compileall .</automated>
  </verify>
  <acceptance_criteria>
    - app.py imports `load_model_or_exit`
    - app.py defines `MODEL_PATH = Path(__file__).parent / "model.pkl"`
    - app.py calls `load_model_or_exit(MODEL_PATH)` at module scope
  </acceptance_criteria>
  <done>App preloads model.pkl at startup and fails fast on load error.</done>
</task>

<task type="auto">
  <name>Task 2: Create pinned requirements.txt for runtime</name>
  <read_first>
    requirements.txt (if exists)
    .planning/PROJECT.md
    .planning/phases/01-core-local-inference-api/01-RESEARCH.md
  </read_first>
  <files>requirements.txt</files>
  <action>
    Create requirements.txt with pinned runtime dependencies:
    - Flask==3.1.1
    - waitress==3.0.2
    - scikit-learn==1.7.1
    - numpy==2.4.4
    - scipy==1.17.1
    - joblib==1.5.3
    Keep the file sorted as listed above, one per line, no extras.
  </action>
  <verify>
    <automated>python -m compileall .</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains Flask==3.1.1
    - requirements.txt contains waitress==3.0.2
    - requirements.txt contains scikit-learn==1.7.1
    - requirements.txt contains numpy==2.4.4, scipy==1.17.1, joblib==1.5.3
  </acceptance_criteria>
  <done>Runtime dependencies are pinned per stack guidance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process startup | Local environment loads model on startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T (Tampering) | requirements.txt | mitigate | Pin exact versions to prevent unexpected dependency drift. |
| T-01-08 | D (DoS) | app.py startup | accept | Fail-fast on model load; local API, no availability SLA. |

</threat_model>

<verification>
- `python -m compileall .` passes.
</verification>

<success_criteria>
- App loads model on startup and requirements.txt is pinned.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-local-inference-api/01-03-SUMMARY.md`.
</output>
